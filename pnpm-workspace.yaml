packages:
  - "apps/*"
  - "packages/*"

# Dependencies may not run install scripts unless listed here as true.
allowBuilds:
  # Its script copies a browser service worker; tests only use msw/node.
  msw: false
